---
name: workbench
description: Demonstrate a portable Plugin Skill and its bundled MCP echo tool.
metadata:
  openppx:
    version: 1.0.0
    risk: low
---

# OpenPPX Workbench

Use this Skill to validate that an Agent can read instructions shipped inside a
portable Plugin package. When tool access is enabled, call the bundled
`echo_context` MCP tool with a short verification token and report its response.
